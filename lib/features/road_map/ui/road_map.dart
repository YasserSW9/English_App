import 'package:flutter/material.dart';

class RoadMap extends StatelessWidget {
  const RoadMap({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("RoadMap")));
  }
}
